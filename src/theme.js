import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FF6F61',
      contrastText: '#fff',
    },
    secondary: {
      main: '#FF6F61',
      contrastText: '#000',
    },
  },
});

export default theme;
